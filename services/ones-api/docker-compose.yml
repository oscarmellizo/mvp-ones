services:
  dynamodb-local:
    image: amazon/dynamodb-local:2.5.2
    command: ["-jar", "DynamoDBLocal.jar", "-inMemory", "-sharedDb"]
    ports:
      - "8000:8000"

  ones-api:
    build:
      context: .
    ports:
      - "8080:8080"
    environment:
      AWS_REGION: us-east-1
      DYNAMODB_ENDPOINT: http://dynamodb-local:8000
      ONES_EVENTS_TABLE: ones-events
      ONES_PLANS_TABLE: ones-plans
      ONES_USER_SUBSCRIPTIONS_TABLE: ones-user-subscriptions
      ONES_MERCADOPAGO_ACCESS_TOKEN: ${ONES_MERCADOPAGO_ACCESS_TOKEN:-}
      ONES_MERCADOPAGO_PUBLIC_KEY: ${ONES_MERCADOPAGO_PUBLIC_KEY:-}
      ONES_MERCADOPAGO_WEBHOOK_SECRET: ${ONES_MERCADOPAGO_WEBHOOK_SECRET:-}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      CORS_ALLOWED_ORIGINS: http://localhost:5173,http://localhost:3000
    depends_on:
      - dynamodb-local
